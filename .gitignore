__pycache__/
*.pyc
.ipynb_checkpoints/
.idea/
.vscode/

# CIFAR-10 batches ARE tracked in this repo (cifar-10-batches-py/), so the
# results reproduce from a clone with no download.
*.tar.gz

# wandb run directory
wandb/

# sweep artefacts are regenerated by sweep.py
sweep_results.json
results_*.json

# NOTE: checkpoints/best_mobilenetv2_cifar10.pth IS tracked (8.5 MB).
# Committing it lets the compression results be reproduced without
# retraining.
.DS_Store
